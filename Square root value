#include <stdio.h>
#include <math.h>
int main() {
    int n ;
    float v;
    printf (" enter the number :") ;
    scanf ("%d" ,&n) ;
    v= sqrt(n) ;
   printf ("the sqrt value: %.1f",v);
   getchar();
    
}
