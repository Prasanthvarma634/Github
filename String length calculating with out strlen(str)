
#include <stdio.h>

int main() {
    char str[100];
    printf ("enter string:");
    fgets(str, sizeof(str),stdin);
    int len =0;
    
    while(str[len]!='\0') len++;
  if(len>0 && str[len-1]=='\n') len--;
    printf ("length:%d\n" , len);
    return 0;
    

}
