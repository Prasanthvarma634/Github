#include<stdio.h>

int min (int a, int b,int c) {

    if((a<b)&&(a<c)) {

        return a;

    }
    else if (b<c) {

        return b;

    }
    else {

        return c;

    }
}
int main () {

int a=10 , b = 20 , c = 30 ; 

printf ("min value : %d\n" ,min(a, b, c)) ;

return 0;

}
