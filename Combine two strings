#include <stdio.h>
#include <string.h>

int main() {
    char c[100];
    char n[50];
printf("enter  th string1:");
fgets(c,sizeof(c),stdin);
c[strlen(c)-1]='\0';
printf("enter the string2:");
fgets (n, sizeof(n),stdin);
printf("%s%s\n",c,n);
    return 0;
}
